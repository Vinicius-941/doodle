object Block

// Caixa sólida: chão, degraus e pilares.
use BoxCollider

var color = 0xB0A898
var texture = ""   // opcional: imagem aplicada em cada face

function draw() {
    draw_mesh(mesh_cube, position, rotation, size, color, texture)   // o mesmo giro do colisor
}
