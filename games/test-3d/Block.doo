object Block

// Caixa sólida: chão, degraus e pilares.
use BoxCollider

var color = 0xB0A898
var texture = ""   // opcional: imagem aplicada em cada face

function draw() {
    draw_mesh(mesh_cube, position, vec3(0, 0, 0), size, color, texture)
}
